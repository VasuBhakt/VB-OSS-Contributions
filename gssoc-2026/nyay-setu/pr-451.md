## Links
- <a href="https://github.com/viru0909-dev/nyay-setu-working/pull/451">Pull Request - 451</a>
- <a href="https://github.com/viru0909-dev/nyay-setu-working/issues/308">Issue - 308</a>

## Problem
### Description

The NLP orchestrator depends on external AI services without any circuit breaker implementation.

If one provider fails, requests may hang and affect the entire system.

### Suggested Action

1. Add retry logic with exponential backoff

2. Implement circuit breaker behavior

3. Return fallback responses during outages

## Solution
Adds resilience to the NLP orchestration layer by implementing a circuit breaker pattern and retry logic for all external AI provider calls (Groq, Gemini, Indian Kanoon). Previously, bare try/except blocks with no retry logic meant transient failures were not retried and sustained outages caused every request to hang until timeout.
