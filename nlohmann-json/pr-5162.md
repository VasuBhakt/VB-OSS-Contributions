## Links
- <a href="https://github.com/nlohmann/json/pull/5162">**Pull Request - 5162**</a>
- <a href="https://github.com/nlohmann/json/issues/5095">**Issue - 5095**</a>

## Description
When creating documentation with Doxygen and Autolinking, `The` becomes a requirement ID and every instance of `The` anywhere else becomes a link to the page.
The reason is that `@requirement` expects the first argument to be an ID for the requirement.

## Solution
- Added unique requirement IDs (`REQ-JSON-01` and `REQ-JSON-02`) to the `@requirement` tags in `iter_impl.hpp` and `json_reverse_iterator.hpp`.
- Updated the amalgamated `single_include/nlohmann/json.hpp` file.
